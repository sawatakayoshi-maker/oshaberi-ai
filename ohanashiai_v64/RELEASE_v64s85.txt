■ v64s85（外部AI解析による洗練：固まらない・漏れない・伝わる）2026-07-21 ※v64s84までを全て含む
--------------------------------------------------
バッジ v64s84→v64s85／sw v417→v418。
全16,462行を静的解析＋自動スモークテスト（22項目）で検証したうえで、以下を改修しました。

【1. 待受の空耳（Whisper幻聴）フィルタを強化】※この版の目玉
・症状：締め言葉が2つ“連なる”幻聴（例「ご視聴ありがとうございました。チャンネル登録
  お願いします。」）が、全体では型に一致せず、すり抜けて自動送信されていた。
・修正：句点等で文に分け【すべての文がそれぞれ締め言葉】のときだけ弾く判定を追加。
  内容のある文が1つでもあれば通す＝本物の発話は巻き込まない（従来方針を維持）。
・検証：単発幻聴6種・複合幻聴3種・本物の発話6種の計12ケースで期待どおりを確認。

【2. 通信ハングでUIが固まる問題の根治（タイムアウト網羅）】
・会話API（callClaude）：90秒タイムアウト追加。Worker/回線が無応答でも
  「考え中」のまま永久フリーズしなくなった（停止ボタンとは両立）。
・文字起こし（STT）4箇所：40秒タイムアウト追加（fetchWithTimeout共通化）。
  「…文字にしています」のまま戻らない事象の対策。
・読み上げ合成（openaiTTS synth）：40秒タイムアウト追加。無応答時は既存の
  「端末の声へフォールバック」に確実に落ちる（無音のまま停止しない）。
・音声テスト再生：30秒タイムアウト＋再生失敗時のURL解放。

【3. メモリリークの解消】
・読み上げ割り込み時、先読み中の音声URLが解放されず蓄積 → 脱出時に必ず解放。
・スキャンPDF保存／動画保存フォールバックのダウンロードURLを保存後に解放。

【4. 見えない失敗を知らせる】
・会話履歴の保存失敗（容量不足等）が無言だった → 状態表示で
  「⚠ 会話の保存に失敗しました…」と知らせる（会話は継続できる）。

【5. 二重実行の穴をふさぐ】
・「デモ」直接起動の経路だけ busy ガードが無く、連打で二重起動しえた → ガード追加。

【6. アクセシビリティ（高齢者・支援技術）】
・状態表示(#status)に aria-live=polite：進捗が読み上げ機器にも伝わる。
・マイク/送るボタンに aria-label、マイクは録音中を aria-pressed で通知（自動同期）。
・全モーダル27個に role="dialog" aria-modal="true" を一括付与。
・Escキーで最前面のモーダルを閉じる（各モーダル既存の「背景クリックで閉じる」
  経路を合成クリックで呼ぶため、後片付けもそのまま実行。IME確定Escは除外）。

【検証】
・全8スクリプトブロック構文チェック：ALL PASS
・自動スモークテスト：22 PASS／3 FAIL は検証環境のCDN遮断由来（素のv64s84でも同結果）／回帰なし
・空耳フィルタ単体テスト：12/12 合格

【今回は見送った指摘（今後の候補）】
・alert/confirm 約140件の平易文・多言語化（技術用語の露出が数カ所残る）
・ダークモード対応（prefers-color-scheme）
・SWキャッシュ名の自動採番化／キャッシュ破損時のネットワークフォールバック
・モーダルのフォーカストラップ（Tab循環）
